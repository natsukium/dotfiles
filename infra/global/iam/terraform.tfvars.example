# IAM User IDs allowed to assume the terraform-state-manager role
# Get your user ID: aws iam get-user --user-name YOUR_USERNAME | jq -r '.User.UserId'
allowed_user_ids = [
  "AIDAXXXXXXXXXXXXXXXXX"  # Replace with actual user ID
]

# Optional: Override default values
# aws_region        = "us-east-2"
# state_bucket_name = "natsukium-tfstate"
# environment       = "global"
# project          = "home-infrastructure"