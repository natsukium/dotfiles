name: web-localextract
version: 1.0.0
description: "Local URL extraction via trafilatura, so web_extract works without a paid API."
author: natsukium
kind: backend
provides_web_providers:
  - localextract
