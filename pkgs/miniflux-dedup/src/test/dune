; Attached to the package so that `dune runtest -p miniflux-dedup`, which is
; how the nix build invokes the suite, picks it up.

(test
 (name test_miniflux_dedup)
 (package miniflux-dedup)
 (libraries miniflux_dedup alcotest qcheck))
